apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Chart.Name }}
  annotations:
    # IRSA: binds the pod to the wildcard-policy role from terraform/main.tf
    eks.amazonaws.com/role-arn: {{ .Values.serviceAccount.roleArn | quote }}
automountServiceAccountToken: true
