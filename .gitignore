node_modules/
qscanner-output/
demo-output/
.qscanner/
*.tar.gz
bin/

# Qualys backend writes this to the CWD (not -o) when a code-asset secret scan's upload path
# isn't supported yet ("Secret scanning is not yet fully supported for code assets").
secret_payload.json

# Terraform local state
.terraform/
.terraform.lock.hcl
